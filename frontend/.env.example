VITE_API_URL=/api
VITE_PROXY_TARGET=https://your-backend-domain.com
# Frontend always uses same-origin /api and relies on Vercel rewrite for production.
VITE_API_URL=/api

# Used by Vite dev proxy in local development.
VITE_PROXY_TARGET=https://smart-expense-tracker-zaxi.onrender.com
